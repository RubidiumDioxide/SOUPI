<MudItem xs="12" sm="6" md="4">
    <MudCard Elevation="25">
        <MudCardHeader>
            <CardHeaderContent>
                <MudText Typo="Typo.h6">@Project.Name</MudText>
            </CardHeaderContent>
            <CardHeaderActions>
                <MudMenu Icon="@Icons.Material.Filled.MoreVert" Dense>
                    @*
                    <MudMenuItem Icon="@Icons.Material.Filled.Edit" IconColor="Color.Default" OnClick="EditButtonClick" Label="Редактировать" />
                    <MudMenuItem Icon="@Icons.Material.Filled.Delete" IconColor="Color.Error" OnClick="DeleteButtonClick" Label="Удалить" />*@
                </MudMenu>
            </CardHeaderActions>
        </MudCardHeader>
        <MudCardContent>
            <MudText Typo=Typo.body2>@Project.Description</MudText>
            <MudText Typo=Typo.body2>Создано @Project.CreationDateTime </MudText>
        </MudCardContent>
        <MudDivider />
        <MudCardActions Class="d-flex">
            <MudButton Variant="Variant.Text" Color="Color.Primary" Class="flex-none">
                Результаты
            </MudButton>
        </MudCardActions>
    </MudCard>
</MudItem>


@code {
    [Parameter, EditorRequired]
    public ProjectDto Project { get; set; } = default!;

    [Parameter, EditorRequired]
    public EventCallback<Guid> OnDeleteButtonClick { get; set; }

    [Parameter, EditorRequired]
    public NavigationManager NavigationManager { get; set; } = default!;

    /*
    public void EditButtonClick()
    {
        NavigationManager.NavigateTo($"/projects/edit/{Project.Id}");
    }

    public async void DeleteButtonClick()
    {
        await OnDeleteButtonClick.InvokeAsync(Survey.Id);
    }*/
}
